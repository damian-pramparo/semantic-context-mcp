# Environment Configuration for Enterprise Code Search MCP

# ===========================================
# EMBEDDING PROVIDER CONFIGURATION
# ===========================================

# Choose embedding provider: 'openai' or 'ollama' (default: ollama)
EMBEDDING_PROVIDER=ollama

# ===========================================
# OPENAI CONFIGURATION (if using OpenAI)
# ===========================================

# OpenAI API Key (required only if EMBEDDING_PROVIDER=openai)
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI embedding model (default: text-embedding-3-small)
OPENAI_MODEL=text-embedding-3-small

# ===========================================
# OLLAMA CONFIGURATION (if using Ollama)
# ===========================================

# Ollama host URL (default: http://localhost:11434)
OLLAMA_HOST=http://localhost:11434

# Ollama embedding model (default: nomic-embed-text)
# Available options: nomic-embed-text, all-minilm, mxbai-embed-large
OLLAMA_MODEL=nomic-embed-text

# ===========================================
# CHROMADB CONFIGURATION
# ===========================================

# ChromaDB host (default: localhost)
CHROMA_HOST=localhost

# ChromaDB port (default: 8000)
CHROMA_PORT=8000

# Collection name for storing embeddings (default: company_codebase)
COLLECTION_NAME=company_codebase

# ===========================================
# COMPANY CONFIGURATION
# ===========================================

# Your company name (used in reports and metadata)
COMPANY_NAME=YourCompany

# ===========================================
# OPTIONAL: GITHUB INTEGRATION
# ===========================================

# GitHub token for private repositories (optional)
# GITHUB_TOKEN=your-github-token-here

# ===========================================
# OPTIONAL: DATABASE CONFIGURATION
# ===========================================

# PostgreSQL password for metadata storage (optional)
POSTGRES_PASSWORD=your_secure_password

# ===========================================
# DOCKER COMPOSE OVERRIDES
# ===========================================

# If running with Docker Compose, these will override the defaults
# CHROMA_HOST=chromadb
# OLLAMA_HOST=http://ollama:11434